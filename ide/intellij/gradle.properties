# SPDX-License-Identifier: Apache-2.0
# Overridden in CI releases with -PpluginVersion=<tag version>.
# x-release-please-start-version
pluginVersion=0.1.0
# x-release-please-end
# The IDE the plugin compiles and tests against; its major version is also the lowest supported
# version (sinceBuild). 2025.3 is the first unified IntelliJ IDEA release, where the LSP API is
# available to every user.
platformVersion=2025.3.6.1
sinceBuild=253
# The Gherkin plugin the plugin compiles against and runIde and the tests install: a Marketplace
# version for platformVersion.
gherkinVersion=253.28294.218
# The Go plugin in the runIdeWithGo sandbox (a build for platformVersion).
goPluginVersion=253.33514.17

org.gradle.caching=true
org.gradle.configuration-cache=true
