# Draft-07 YAML schema backend, inline fixtures.
factory:
  family: json
  schema: ../schemas/limits.schema.yaml
fixtures:
  gold-tier:
    tier: gold
    ceiling:
      amount: 5000.0
      currency: USD
  base-tier:
    tier: base
