# The downstream address service for the evals: the plain WireMock image with
# the parcels service's stubs baked in. Build context: evals/app/wiremock.
FROM wiremock/wiremock:3.13.2
COPY mappings /home/wiremock/mappings
