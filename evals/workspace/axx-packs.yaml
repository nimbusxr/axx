# The packs this project uses. Manage it with `axx pack add|remove`.
# axx's packs by name (`axx pack list`), others by path (./steps) or Go module (host/path@version).
packs:
  - rest
  - mock
  - sql
  - mongo
  - kafka
