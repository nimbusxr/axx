# What the verifier checks for this task (see evals/README.md, "How a task is verified").
allowed = ["features/**", "seeds/**"]
min_scenarios = 2
mutants = ["import-drops-postcode", "import-leaves-line-pending", "import-wrong-source", "import-accepts-overweight"]
