# Generated by `go run ./cmd/evals sync` from task.toml and the starting project; do not edit.
# The agent's environment: the task's starting project in /app on the evals base image.
FROM axx-evals-base:latest
RUN project-setup common
# The runner adds a condition's agent aids (--agents-md, --skills) to the next line.
RUN project-setup finish
